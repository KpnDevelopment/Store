<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Activity.Setting_Activity"></activity>
        <activity android:name="Adapter.History_Adapter" />
        <activity android:name="Activity.History_Activity" />
        <activity android:name="Activity.Update_Activity" />
        <activity android:name="Adapter.Adapter_Activity" />
        <activity android:name="Activity.Inserting_Activity" />
        <activity android:name="Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>